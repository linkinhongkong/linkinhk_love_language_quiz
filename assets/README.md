# /assets — static image assets

Drop hero / OG / blog images in this folder. They're served at
`https://uat.linkinhk.com/assets/<filename>` (or `https://www.linkinhk.com/assets/<filename>` on prod).

Currently referenced:
- `hero.jpg` — homepage hero image (4:5 aspect ratio, ~1000×1250 px recommended)
